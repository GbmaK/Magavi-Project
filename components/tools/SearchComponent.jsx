import React from "react";
import { useState } from "react";

const Search = (props) => {

  const [userSearch, setSearch] = useState(props.search)
  const [userFilter1, setFilter1] = useState(props.filter1) 

  return (
    <div>
        
    </div>
  )
};

export default Search;
